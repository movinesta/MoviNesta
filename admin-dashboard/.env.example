# Admin Dashboard environment variables (Vite)
#
# Copy to .env.local (or .env) and fill in values.
#
# IMPORTANT: these are PUBLIC client values (anon/publishable key), not the service role key.
# All admin privileges are still enforced server-side by Supabase Edge Functions.

VITE_SUPABASE_URL=
# Either set this:
VITE_SUPABASE_ANON_KEY=
# ...or set this alternative name (supported by the code):
# VITE_SUPABASE_PUBLISHABLE_OR_ANON_KEY=

# Optional branding
VITE_APP_NAME=MoviNesta Admin
